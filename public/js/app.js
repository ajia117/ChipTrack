; //exit any previously loaded js

//Main funct.
jQuery(function($) {
    'use strict';

    /*
     * IO Handling
     * -----------
     * Handles IO 
     */
    var IO = {

    };

    /*
     *
     */
    var App = {

    };

    IO.init();
    App.init();

}($));
